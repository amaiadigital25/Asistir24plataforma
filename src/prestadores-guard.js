function patchProviderGuard(source){let fixed=source;const authMarker='    next();\n  } catch {';if(fixed.includes(authMarker)&&!fixed.includes('Acceso de prestador limitado al módulo de facturación')){fixed=fixed.replace(authMarker,'    if (req.user.role === "prestador" && req.path !== "/api/me" && !req.path.startsWith("/api/prestadores/")) return res.status(403).json({ error: "Acceso de prestador limitado al módulo de facturación" });\n    next();\n  } catch {');}const routeMarker='app.get("/prestador", (req, res) => res.sendFile(path.join(__dirname, "public", "prestador.html")));';if(fixed.includes(routeMarker)&&!fixed.includes('app.get("/prestador-acceso"')){fixed=fixed.replace(routeMarker,'app.get("/prestador-acceso", (req, res) => res.sendFile(path.join(__dirname, "public", "prestador-acceso.html")));\n'+routeMarker);}return fixed;}module.exports={patchProviderGuard};